# Local override. Do not commit personal credentials.
spring.datasource.url=jdbc:postgresql://localhost:5432/oneenterprise_tenant_db
spring.datasource.username=postgres
# Set DB_PASSWORD as an environment variable, or replace this value locally only.
spring.datasource.password=${DB_PASSWORD}
